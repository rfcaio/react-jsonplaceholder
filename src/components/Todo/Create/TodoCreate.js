import React from 'react'

import TodoForm from '../Form'

const TodoCreate = () => {
  return (
    <TodoForm
      title="Create todo"
      onSubmit={() => null}
    />
  )
}

export default TodoCreate
